package com.example.keepit.activities;import android.content.Intent;import android.os.Bundle;import android.widget.Button;import android.widget.EditText;import android.widget.Toast;import androidx.appcompat.app.AppCompatActivity;import com.example.keepit.R;import com.example.keepit.database.UsersDatabase;public class SignupActivity extends AppCompatActivity {    private UsersDatabase usersDatabase;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_signup);        usersDatabase = new UsersDatabase(this);        EditText email = findViewById(R.id.et_email);        EditText pass = findViewById(R.id.et_password);        EditText pass_conf = findViewById(R.id.et_confirm_password);        Button signin = findViewById(R.id.btn_signin);        Button cancel = findViewById(R.id.btn_cancel);        signin.setOnClickListener(v -> {            String emailInput = email.getText().toString().trim();            String passwordInput = pass.getText().toString().trim();            String confirmPasswordInput = pass_conf.getText().toString().trim();            if (emailInput.isEmpty() || passwordInput.isEmpty() || confirmPasswordInput.isEmpty()) {                Toast.makeText(SignupActivity.this, "Please fill all the fields", Toast.LENGTH_SHORT).show();                return;            }            if (!passwordInput.equals(confirmPasswordInput)) {                Toast.makeText(SignupActivity.this, "Passwords do not match", Toast.LENGTH_SHORT).show();                return;            }            // نحاول إنشاء مستخدم جديد            boolean isCreated = usersDatabase.createUser(emailInput, passwordInput);            if (isCreated) {                Toast.makeText(SignupActivity.this, "Account created successfully", Toast.LENGTH_SHORT).show();                // بعد إنشاء الحساب، نذهب إلى شاشة تسجيل الدخول                Intent intent = new Intent(SignupActivity.this, LoginActivity.class);                startActivity(intent);                finish();            } else {                Toast.makeText(SignupActivity.this, "Failed to create account. Try another email.", Toast.LENGTH_SHORT).show();            }        });        cancel.setOnClickListener(v -> {            Intent intent = new Intent(SignupActivity.this, LoginActivity.class);            startActivity(intent);            finish();        });    }}